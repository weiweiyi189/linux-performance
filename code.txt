free_output = run_command("free -m")
    memory_lines = free_output.split('\n')

    # Parse memory usage data
    total_memory_line = memory_lines[1].split()
    used_memory_line = memory_lines[2].split()
    total_memory = int(total_memory_line[1])
    used_memory = int(used_memory_line[2])
    free_memory = total_memory - used_memory

    # Visualize memory usage breakdown in a pie chart
    pie = Pie("Memory Usage Breakdown")
    pie.add("", ["Used Memory", "Free Memory"], [used_memory, free_memory],
            radius=["40%", "75%"], center=["50%", "50%"], rosetype="radius")
    pie.render("memory_usage.html")